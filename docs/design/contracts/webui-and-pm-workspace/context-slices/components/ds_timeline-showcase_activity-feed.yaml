ds_id: ds:timeline-showcase:activity-feed
type: ActivityFeed
parent_ds_id: ds:timeline-showcase:surface
actions: []
screen_id: screen:ds-timeline
screen_route: /design-system/timeline
screen_ds_id: ds:screen:timeline-showcase-001
prd_ds_id: ds:screen:timeline-001
states:
- default
- loading
- empty
- error
- offline
- forbidden
bindings:
- GET /api/activity
- GET /api/file-leases
- GET /api/tasks/:id/activity
layout: file lease indicators, activity feed, and sprint day timeline
behaviors:
- show unlocked, locked, expiring, and expired lease states
- hash anchors for file-lease, activity-feed, and sprint-day
- update from activity polling every 3 to 5 seconds
api_gateway: gmind serve Go REST API
hash_route_interactions:
- hash anchors for file-lease, activity-feed, and sprint-day
