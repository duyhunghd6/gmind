ds_id: ds:approval-showcase:evidence
type: EvidenceBlocks
parent_ds_id: ds:approval-showcase:surface
actions: []
screen_id: screen:ds-approval
screen_route: /design-system/approval
screen_ds_id: ds:screen:approval-showcase-001
prd_ds_id: ds:screen:approval-001
states:
- default
- loading
- empty
- error
- offline
- forbidden
bindings:
- GET /api/tasks?status=pending-approval
- GET /api/coverage
- GET /api/approval/:id/evidence
- POST /api/approval/:id/decision
layout: approval panels with status toggles, evidence blocks, RTM matrix, and coverage
  heatmap
behaviors:
- pending, approved, and rejected toggles
- evidence blocks for Tests, Diff, Beads ID, PRD, and CI
- hash anchors for panels, RTM, and heatmap
api_gateway: gmind serve Go REST API
hash_route_interactions:
- hash anchors for panels, RTM, and heatmap
