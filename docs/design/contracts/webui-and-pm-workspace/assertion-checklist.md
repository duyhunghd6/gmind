<!-- beads-id: br-design-assertions-webui-pm-workspace -->
# Assertion Checklist: WebUI PM Workspace

Generated from the current block-style YAML View Blueprint in `ui-contract.md`; every row below is a current contract screen and every `ds_id` is copied from that contract.

| Screen | Route | Contract ds_id | States | API endpoints | Component ds_ids | Assertions |
| --- | --- | --- | --- | --- | --- | --- |
| `screen:global-shell` | `/design-system/webui-pm-workspace` | `ds:screen:webui-pm-workspace-001` | `default, loading, empty, error, offline, forbidden, saving, sync_conflict` | `GET /api/health; GET /api/coverage; GET /api/tasks; GET /api/trace/:id; GET /api/docs; GET /api/search` | `ds:global-shell:root; ds:global-shell:header; ds:global-shell:sidebar; ds:global-shell:active-surface; ds:global-shell:sync-banner` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:rtm-dashboard` | `/` | `ds:screen:rtm-dashboard-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/coverage; GET /api/tasks; GET /api/trace/:id?depth=2; GET /api/gaps` | `ds:rtm-dashboard:root; ds:rtm-dashboard:kpis; ds:rtm-dashboard:coverage-heatmap; ds:rtm-dashboard:task-progress; ds:rtm-dashboard:knowledge-graph-widget; ds:rtm-dashboard:gap-analysis` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:safe-board` | `/board` | `ds:screen:kanban-001` | `default, loading, empty, error, offline, forbidden, saving` | `GET /api/tasks?view=board&board=<id>; PUT /api/tasks/:id/status; GET /api/tasks/:id/activity` | `ds:kanban:root; ds:kanban:board-selector; ds:kanban:columns; ds:kanban:stats; ds:kanban:rte-escalation-badge` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:task-list` | `/tasks` | `ds:screen:task-list-001` | `default, loading, empty, error, offline, forbidden, saving` | `GET /api/tasks?format=list; PUT /api/tasks/bulk` | `ds:task-list:root; ds:task-list:filters; ds:task-list:table; ds:task-list:bulk-actions` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:task-detail` | `/tasks/:id` | `ds:screen:task-detail-001` | `default, loading, empty, error, offline, forbidden, saving, not_found` | `GET /api/tasks/:id; GET /api/tasks/:id/activity; GET /api/trace/:id?depth=2; PUT /api/tasks/:id` | `ds:task-detail:root; ds:task-detail:editable-fields; ds:task-detail:tabs; ds:task-detail:activity; ds:task-detail:graph-widget` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:approval-gates` | `/approval` | `ds:screen:approval-001` | `default, loading, empty, error, offline, forbidden, insufficient_evidence, decision_submitted` | `GET /api/tasks?status=pending-approval; GET /api/approval/:id/evidence; GET /api/coverage; POST /api/approval/:id/decision` | `ds:approval:root; ds:approval:queue; ds:approval:evidence-hub; ds:approval:rtm-matrix; ds:approval:decision-controls` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:doc-viewer` | `/docs` | `ds:screen:doc-viewer-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/docs?group=source_type; GET /api/docs/:id; GET /api/coverage?doc=<id>` | `ds:doc-viewer:root; ds:doc-viewer:tree; ds:doc-viewer:content; ds:doc-viewer:section-badges` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:trace-explorer` | `/trace/:id` | `ds:screen:trace-explorer-001` | `default, loading, empty, error, offline, forbidden, partial` | `GET /api/trace/:id?depth=full; GET /api/graph/presets; GET /api/impact/:section` | `ds:trace-explorer:root; ds:trace-explorer:toolbar; ds:trace-explorer:graph-canvas; ds:trace-explorer:dag-layers; ds:trace-explorer:detail-panel` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:search-explorer` | `/search` | `ds:screen:explorer-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/search?q=<query>&type=<type>` | `ds:search-explorer:root; ds:search-explorer:input; ds:search-explorer:type-filters; ds:search-explorer:results` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:terminal-console` | `/terminal` | `ds:screen:terminal-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/agents/sessions; GET /api/ci/runs; GET /api/tasks/:id/activity; STREAM /api/log-events?stream=terminal` | `ds:terminal:root; ds:terminal:scenario-tabs; ds:terminal:mosaic; ds:terminal:line-list` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:portfolio` | `/portfolio` | `ds:screen:portfolio-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/portfolio/epics; GET /api/tasks?issue_type=epic` | `ds:portfolio:root; ds:portfolio:epic-table; ds:portfolio:roadmap` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:pi-planning` | `/pi-planning` | `ds:screen:pi-planning-001` | `default, loading, empty, error, offline, forbidden, saving` | `GET /api/pi/features; PUT /api/pi/plan; GET /api/risks?view=roam; POST /api/pi/confidence-vote` | `ds:pi-planning:root; ds:pi-planning:strategic-sandbox; ds:pi-planning:value-scoring; ds:pi-planning:confidence-vote; ds:pi-planning:roam-board` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:git-graph` | `/git-graph` | `ds:screen:git-graph-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/git/graph?scenario=<id>; GET /api/trace/:id?include=git` | `ds:git-graph:root; ds:git-graph:scenario-selector; ds:git-graph:canvas; ds:git-graph:commit-detail` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:timeline` | `/timeline` | `ds:screen:timeline-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/activity; GET /api/file-leases; GET /api/tasks/:id/activity` | `ds:timeline:root; ds:timeline:file-lease-panel; ds:timeline:activity-feed; ds:timeline:sprint-day` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:components-catalog` | `/design-system/components` | `ds:screen:components-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/design-system/components` | `ds:components-catalog:root; ds:components-catalog:section-nav; ds:components-catalog:examples` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:storyboard-overview` | `/storyboards` | `ds:screen:storyboard-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/storyboards` | `ds:storyboard-overview:root; ds:storyboard-overview:filter; ds:storyboard-overview:flow; ds:storyboard-overview:guidance` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:storyboard-detail` | `/storyboards/:id` | `ds:screen:storyboard-detail-001` | `default, loading, empty, error, offline, forbidden, not_found` | `GET /api/storyboards/:id` | `ds:storyboard-detail:root; ds:storyboard-detail:role-panel; ds:storyboard-detail:step-timeline; ds:storyboard-detail:related-usecases` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. |
| `screen:document-graph-widget` | `embedded` | `ds:screen:document-graph-widget-001` | `default, loading, empty, error, offline, forbidden` | `GET /api/trace/:id?depth=2` | `ds:document-graph-widget:root; ds:document-graph-widget:canvas; ds:document-graph-widget:detail-panel` | data-screen-id matches; data-ds-id matches; route/hash anchors preserve shell state; browser data comes from Go REST API only; boundary states include explicit recovery affordances. Embedded widget explicitly declares default/loading/empty/error/offline/forbidden local graph states while inheriting host shell recovery. |
